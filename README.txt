Requisitos: Ter o docker instalado

Para executar o projeto, basta digitar a linha de comando na pasta onde se encontra o docker-compose

Linha de comando:
docker-compose up -d

Url de acesso ao swagger:
http://localhost:8080/swagger/index.html